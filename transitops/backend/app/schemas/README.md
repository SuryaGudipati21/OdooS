Pydantic request/response models go here, mirroring models/ (same file owners:
user.py/vehicle.py/driver.py/fuel_expense.py -> Dev A, trip.py/maintenance.py -> Dev B).
Not scaffolded with placeholder code since these are quick to write once the
SQLAlchemy models above are final — copy the field list and add validators.
